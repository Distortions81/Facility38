DETECT WINDOWS, DISABLE VSYNC

Optimize: Make uninterrupted sections of belt one entity.
Cache toolbar UI, overlay for button press or selection.

https://github.com/Distortions81/GameTest/issues/1

Check on belt blinking when miner replaced

** Draw a ghost for pending delete/add object **
make belt juctions

1:
make occupied map

2:
instead of maps, use 32 line blocks
chunks become multi line objects
do this so we can render and process
left to right, top to bottom

3:
add tick lists, and divide up work for every nth tick loads

